# Gem Linux-specific overrides
version: 0.1

# Linux-specific directory configuration
directories:
  config:
    path: "~/.gem"
    mode: "0755"
  data:
    path: "~/.gem/ruby/*/gems/{{ software_name }}-*"
    mode: "0755"

# Linux-specific file configuration
files:
  config:
    path: "~/.gemrc"