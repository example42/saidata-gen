# Gem macOS-specific overrides
version: 0.1

# macOS-specific directory configuration
directories:
  config:
    path: "~/.gem"
    mode: "0755"
  data:
    path: "~/.gem/ruby/*/gems/{{ software_name }}-*"
    mode: "0755"

# macOS-specific file configuration
files:
  config:
    path: "~/.gemrc"