# Helm macOS-specific overrides
version: 0.1

# macOS-specific directory configuration
directories:
  config:
    path: "~/.helm"
    mode: "0755"
  data:
    path: "~/Library/Caches/helm"
    mode: "0755"

# macOS-specific file configuration
files:
  config:
    path: "~/.helm/repositories.yaml"