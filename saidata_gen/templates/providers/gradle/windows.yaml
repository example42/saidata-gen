# Gradle Windows-specific overrides
version: 0.1

# Windows-specific directory configuration
directories:
  config:
    path: "%USERPROFILE%\\.gradle"
    owner: Administrator
    group: Administrators
    mode: "0644"
  data:
    path: "%USERPROFILE%\\.gradle\\caches"
    owner: Administrator
    group: Administrators
    mode: "0644"

# Windows-specific file configuration
files:
  config:
    path: "%USERPROFILE%\\.gradle\\gradle.properties"
    owner: Administrator
    group: Administrators
    mode: "0644"