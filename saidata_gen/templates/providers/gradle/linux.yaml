# Gradle Linux-specific overrides
version: 0.1

# Linux-specific directory configuration
directories:
  config:
    path: "~/.gradle"
    mode: "0755"
  data:
    path: "~/.gradle/caches"
    mode: "0755"

# Linux-specific file configuration
files:
  config:
    path: "~/.gradle/gradle.properties"