# Cargo macOS-specific overrides
version: 0.1

# macOS-specific directory configuration
directories:
  config:
    path: "~/.cargo"
    mode: "0755"
  data:
    path: "~/.cargo/bin"
    mode: "0755"

# macOS-specific file configuration
files:
  config:
    path: "~/.cargo/config.toml"