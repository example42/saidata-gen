# Maven macOS-specific overrides
version: 0.1

# macOS-specific directory configuration
directories:
  config:
    path: "~/.m2"
    mode: "0755"
  data:
    path: "~/.m2/repository"
    mode: "0755"

# macOS-specific file configuration
files:
  config:
    path: "~/.m2/settings.xml"