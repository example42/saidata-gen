# Go Linux-specific overrides
version: 0.1

# Linux-specific directory configuration
directories:
  config:
    path: "~/go"
    mode: "0755"
  data:
    path: "~/go/bin"
    mode: "0755"

# Linux-specific file configuration
files:
  config:
    path: "~/go/go.mod"