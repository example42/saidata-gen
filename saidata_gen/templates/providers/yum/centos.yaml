# CentOS-specific YUM overrides
version: 0.1

# CentOS-specific URL
urls:
  yum: "https://centos.pkgs.org/{{ software_name }}"

# CentOS-specific repository configuration
files:
  repo:
    path: "/etc/yum.repos.d/{{ software_name }}.repo"
    owner: root
    group: root
    mode: "0644"