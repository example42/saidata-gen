# Nixpkgs Linux-specific overrides
version: 0.1

# Linux-specific directory configuration
directories:
  config:
    path: "~/.config/nixpkgs"
    mode: "0755"
  data:
    path: "/nix/store"
    mode: "0755"

# Linux-specific file configuration
files:
  config:
    path: "~/.config/nixpkgs/config.nix"